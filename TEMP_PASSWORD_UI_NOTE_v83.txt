v83 Team Members UI patch

What changed:
- Added visible 'Issue Temp Password' button to each team member card
- Admin only, Firebase Live only
- Disabled for the current signed-in admin user
- Shows simple status chips when a user still needs to change password or has a temporary password flag

Important:
- The button uses the existing Cloud Function issueTemporaryPassword
- Deploy functions/index.js and firestore.rules if not already deployed
